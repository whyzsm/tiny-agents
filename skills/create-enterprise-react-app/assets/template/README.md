# **PROJECT_TITLE**

An enterprise React application shell generated from the Codex project baseline.

## Commands

```bash
pnpm install
pnpm dev
pnpm check
pnpm test:e2e
pnpm storybook
```

The blank route is intentionally small. Add business capabilities under
`src/features` and keep API calls behind `src/services`.
