interface:
  display_name: "skill生成器"
  short_description: "先查找已有能力，再生成可验证的 Codex Skill 包"
  default_prompt: "Use $skill-generation-workbench to search find-skills first, search SkillHub second, then design or upgrade a production-ready Skill package in Simplified Chinese without installing candidates."
policy:
  allow_implicit_invocation: true
