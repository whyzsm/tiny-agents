interface:
  display_name: Transcript to Content
  short_description: This skill transforms training and onboarding meeting transcript
  default_prompt: Use $transcript-to-content to complete this workflow and produce
    concrete deliverables.
policy:
  allow_implicit_invocation: true
